[project]
name = "poker-engine"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["poker_engine*"]

[tool.pytest.ini_options]
testpaths = ["tests"]